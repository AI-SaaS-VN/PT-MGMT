# 开发 / 测试专用依赖（不进入生产镜像）
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1                      # TestClient 依赖
pytest-cov==6.0.0
ruff==0.8.4                        # Linter + Formatter
mypy==1.13.0
